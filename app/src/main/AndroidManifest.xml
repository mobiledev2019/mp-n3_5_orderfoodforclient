<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="vn.ngoctrung.order">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:theme="@style/AppTheme" >

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity
            android:name=".MenuActivity"
            android:label="@string/title_activity_menu" >
        </activity>
        <activity
            android:name=".ConfirmMenuActivity"
            android:label="@string/title_activity_confirm_menu" >
        </activity>
        <activity
            android:name=".BillActivity"
            android:label="@string/title_activity_bill" >
        </activity>
        <activity
            android:name=".ChangeMenuActivity"
            android:label="@string/title_activity_change_menu" >
        </activity>
        <activity
            android:name=".ConfirmChangeMenuActivity"
            android:label="@string/title_activity_confirm_change_menu" >
        </activity>
    </application>

</manifest>